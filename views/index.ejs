<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Role</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        /* Background image styling */
        .bg-image {
            background-image: url('/uploads/bg.png'); /* Background image path */
            background-size: cover; /* Cover the screen */
            background-repeat: no-repeat; /* No repeat */
            background-position: center; /* Center the image */
            height: 100vh; /* Full viewport height */
            width: 100vw; /* Full viewport width */
        }

        /* Container styling for overlay */
        .overlay-container {
            background-color: rgba(255, 255, 255, 0.8); /* Light semi-transparent white background */
            border-radius: 12px;
            padding: 3rem;
            max-width: 500px;
            width: 100%;
            text-align: center;
        }

        /* Button hover and transition effects */
        .btn {
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.2);
        }

        /* Styling for the buttons */
        .btn-seller {
            background: linear-gradient(to right, #4e8ef7, #3571b9); /* Blue gradient for Seller */
            color: white;
        }

        .btn-buyer {
            background: linear-gradient(to right, #4ade80, #2e8d58); /* Green gradient for Buyer */
            color: white;
        }

        /* Footer styling */
        .footer-text {
            font-size: 0.875rem;
            color: #333;
        }
    </style>
</head>

<body class="bg-image flex justify-center items-center">

    <!-- Overlay container -->
    <div class="overlay-container">
        <!-- Header -->
        <h1 class="text-4xl font-bold text-gray-800 mb-4">CUET T Sell & Buy</h1>
        <p class="text-gray-600 mb-8">Please continue as a buyer or seller to proceed.</p>

        <!-- Role Buttons -->
        <a href="/auth/seller" class="btn btn-seller block w-full py-3 mb-4 text-lg font-semibold rounded-lg shadow-lg">
            Continue as Seller
        </a>
        <a href="/auth/buyer" class="btn btn-buyer block w-full py-3 text-lg font-semibold rounded-lg shadow-lg">
            Continue as Buyer
        </a>

        <!-- Footer -->
        <p class="footer-text mt-6">
            Need help? <a href="/support" class="text-blue-500 hover:underline">Contact Support</a>
        </p>
    </div>

</body>

</html>
