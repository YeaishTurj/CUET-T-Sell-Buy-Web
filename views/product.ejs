<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <style>
        /* General Styling */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9fafb;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        h1 {
            color: #3a3a3a;
            font-size: 36px;
            text-align: center;
            margin-bottom: 40px;
        }

        h2 {
            color: #495057;
            font-size: 24px;
            text-align: center;
            margin-bottom: 30px;
        }

        /* Header */
        header {
            background-color: #343a40;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px;
            margin-bottom: 40px;
        }

        /* Navbar */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #343a40;
            padding: 10px 20px;
            border-radius: 5px;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-size: 16px;
            padding: 8px 15px;
            border-radius: 5px;
        }

        nav a:hover {
            background-color: #495057;
        }

        /* Products List Section */
        .product-list {
            display: grid;
            grid-template-columns: repeat(4, 1fr); /* Display 4 cards per row */
            gap: 30px;
        }

        .product-item {
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 15px; /* Reduced padding */
            text-align: center;
            overflow: hidden;
            transition: all 0.3s ease;
            height: 300px; /* Smaller height for the cards */
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            position: relative;
            cursor: pointer;
            text-decoration: none;
        }

        .product-item:hover {
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            transform: translateY(-10px);
        }

        .product-item img {
            width: 100%;
            height: 200px; /* Reduced image height */
            object-fit: cover;
            border-radius: 10px;
        }

        .product-item h4 {
            font-size: 20px; /* Slightly smaller font size */
            color: #343a40;
            margin: 5px 0;
        }

        .product-item .product-type {
            position: absolute;
            top: 10px;
            left: 10px;
            background: linear-gradient(45deg, #1d3557, #2a9d8f);
            color: white;
            font-weight: bold;
            padding: 8px 12px;
            border-radius: 20px;
            font-size: 12px; /* Smaller font size */
            text-transform: uppercase;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
        }

        .product-item p {
            font-size: 16px; /* Slightly smaller font size */
            color: #495057;
        }


        .no-products {
            font-size: 18px;
            color: #495057;
            text-align: center;
            margin-top: 30px;
        }

        /* Footer */
        footer {
            background-color: #343a40;
            color: white;
            text-align: center;
            padding: 15px;
            margin-top: 50px;
            font-size: 14px;
        }
    </style>
</head>

<body>

    <div class="container">

        <!-- Main Content Section -->
        <div>
            <header>
                <!-- Navbar Section -->
                <nav>
                    <a href="http://localhost:55000/buyer-dashboard">Dashboard</a>
                    <a href="/logout">Log Out</a>
                </nav>
            </header>

            <h2>Explore Our Amazing Products</h2>

            <!-- Products List Section -->
            <div class="product-list">
                <% if (products.length > 0) { %>
                    <% products.forEach(product => { %>
                        <a href="/<%= product.op_type === 'new' ? 'new-products' : 'old-products' %>/<%= product.op_type === 'new' ? product.p_id : product.op_id %>" class="product-item">
                            <div class="product-type">
                                <%= product.op_type === 'new' ? 'New Product' : 'Old Product' %>
                            </div>

                            <div>
                                <img src="<%= product.image %>" alt="<%= product.op_title %>">
                            </div>

                            <h4>
                                <%= product.op_type === 'new' ? product.p_title : product.op_title %>
                            </h4>

                            <% if (product.op_type === 'new') { %>
                                <p><strong>Price:</strong> <%= product.price %> Taka</p>
                            <% } else if (product.op_type === 'old') { %>
                                <p><strong>Asking Price:</strong> <%= product.asking_price %> Taka</p>
                            <% } %>

                        </a>
                    <% }) %>
                <% } else { %>
                    <p class="no-products">No products available at the moment.</p>
                <% } %>
            </div>
        </div>

    </div>

    <!-- Footer Section -->
    <footer>
        <p>© 2024 Product Dashboard. All Rights Reserved.</p>
    </footer>

</body>

</html>
